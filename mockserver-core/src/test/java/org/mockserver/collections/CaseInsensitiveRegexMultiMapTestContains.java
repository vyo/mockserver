package org.mockserver.collections;

import org.junit.Test;

import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.empty;
import static org.hamcrest.collection.IsIterableContainingInAnyOrder.containsInAnyOrder;
import static org.mockserver.collections.CaseInsensitiveRegexMultiMap.multiMap;
import static org.mockserver.model.NottableString.string;

/**
 * @author jamesdbloom
 */
public class CaseInsensitiveRegexMultiMapTestContains {

    // contains
//    public boolean containsAll(CaseInsensitiveRegexMultiMap subSet);
//    public boolean containsKeyValue(NottableString key, NottableString value);
//    public boolean containsValue(Object value);
//    public boolean containsKey(Object key);

    // copy test set for nottable strings
}
